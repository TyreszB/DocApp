const NavBar = () => {
  return (
    <nav className="w-full flex justify-center items-center border-b border-[#1f2e1c] px-4 py-4">
      <span className="text-sm font-bold tracking-[0.25em] uppercase text-[#00ff41]">
        DocApp
      </span>
    </nav>
  );
};

export default NavBar;
